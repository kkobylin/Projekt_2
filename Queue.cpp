/*Krzysztof Kobyliñski
Projekt 1.10. na PROI*/

#include <iostream>
#include "Queue.h"
#include <cstdlib>

/*using namespace std;

Que::Que()
{
    qfirst = qlast = nullptr;
    size=0;
}

Que::~Que()
{
    Element * p;
    while(qfirst)
    {
        p=qfirst->next;
        delete qfirst;
        qfirst = p;
    }
}

int size()
{
    return size;
}

Element * add_front(key_type tkey, type_value tvalue)
{
    element
    Element p;
    p.key=tkey;
    p.value=tvalue;
}


    void add(const T& val){
        element* newElement = new element(val);
        if(this->root == nullptr) this->root = newElement;
        else{
            newElement->next = this->root;
            this->root->previous = newElement;
            this->root = newElement;
        }
    }
*/
